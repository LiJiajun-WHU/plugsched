diff --git a/scheduler/rebuild.c b/scheduler/rebuild.c
index xxxxx..yyyyy 100644
--- a/scheduler/rebuild.c
+++ b/scheduler/rebuild.c
@@ -100,6 +100,11 @@ int rebuild_scheduler_state(void)
 rebuild_cfs_rq(rq);
 rebuild_rt_rq(rq);
 rebuild_dl_rq(rq);
+
+/* EEVDF specific rebuild */
+if (rq->cfs.curr)
+update_deadline(rq->cfs.curr);
 }
 
 return 0;
